{
  "name": "mi-repartidor-server",
  "version": "1.0.0",
  "description": "JSON Server for Fast shipping API simulation",
  "scripts": {
    "start": "json-server --watch db.json --port 3000 --routes routes.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
